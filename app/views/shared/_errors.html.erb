  <% if obj.errors.any? %>
  <% obj.errors.full_messages.each do |message| %>
  <%= js add_gritter(message, :image => :warning, :title => "Pay attention!", :fade_out_speed => "slow", :time => 5000, :sticky => false)%>
  <% end %>
  <% end %>
