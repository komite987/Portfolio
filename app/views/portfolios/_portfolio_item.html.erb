  <h3><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item)%></h3>
  <h4><%= portfolio_item.subtitle %></h4>
  <h5><%= portfolio_item.body %></h5>
  <img src="<%= portfolio_item.thumb_img %>"> <br>

  <%= link_to 'Edit', edit_portfolio_path(portfolio_item) %> | 
  <%= link_to 'Delete', portfolio_path(portfolio_item), method: :delete, data: { confirm: 'Are you sure?'} %>
